{
  "name": "nudge-mobile-push",
  "version": "1.0.0",
  "private": true,
  "description": "Web Push relay + pairing PWA so the Nudge extension's tag notifications can reach a phone.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "generate-vapid": "node generate-vapid.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "web-push": "^3.6.7"
  }
}
