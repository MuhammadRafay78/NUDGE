{
  "name": "nudge-mobile-push-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers deployment of the Nudge mobile push relay.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate-vapid": "node ../generate-vapid.js"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
